parameters:
  kapitan:
    vars:
      target: acs-engine-autoscaler
      namespace: temp
    compile:
      - output_path: chart/acs
        input_type: helm
        input_paths:
          - charts/acs-engine-autoscaler
