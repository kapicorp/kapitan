parameters:
  kapitan:
    vars:
      target: nginx-ingress
      namespace: temp
    compile:
      - output_path: chart
        input_type: helm
        input_paths:
          - charts/nginx-ingress
